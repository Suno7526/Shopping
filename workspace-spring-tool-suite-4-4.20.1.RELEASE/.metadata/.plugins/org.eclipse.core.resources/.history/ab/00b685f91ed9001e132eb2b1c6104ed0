package com.example.estate.service;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import com.example.estate.entity.Product;
import com.example.estate.repository.ProductRepository;

@Service
public class ProductService {

 @Autowired
 private ProductRepository productRepository;

 @Transactional
 public void saveProduct(MultipartFile file, String productName, String infomation, String productPrice) {
     Product product = new Product();
     product.setProductName(productName);
     product.setInfomation(infomation);
     product.setProductPrice(productPrice);

     // 파일 처리를 위한 로직을 추가하십시오.
     // 예를 들어, 파일을 서버에 저장하거나 다른 작업을 수행할 수 있습니다.
     // file.getBytes()를 사용하여 파일의 byte 배열을 얻을 수 있습니다.
     // 예시: byte[] fileBytes = file.getBytes();

     productRepository.save(product);
 }
 
 
 @Transactional(readOnly = true)
 public List<Product> getAllProducts() {
     return productRepository.findAll();
 }
 
 
}
